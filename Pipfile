[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
numpy = ">=1.17"
pandas = ">=0.25.0"
scikit-learn = ">=0.21.0"
matplotlib = ">=3.1"
tensorflow = ">=2.0"
click = ">=7.0"
pytan = ">=0.1.0"
seaborn = "*"

[requires]
python_version = "3.7"

[scripts]
signscan = "python -m signscan"
